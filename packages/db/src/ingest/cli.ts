// MPD ingestion CLI. Implementation lands in Phase 4 F11.
// Planned usage: pnpm --filter @statify/db db:ingest --slices <N> --resume

console.log('ingest CLI: not yet implemented (Phase 4 F11)');
process.exit(0);
